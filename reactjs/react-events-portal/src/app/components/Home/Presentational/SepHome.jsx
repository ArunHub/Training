import React from "react";

const SepHome = (props) => {

    return (
        <div>
            <h1>{props.title}</h1>
            <h5>{props.subtitle}</h5>
        </div>
    )
}

export default SepHome;